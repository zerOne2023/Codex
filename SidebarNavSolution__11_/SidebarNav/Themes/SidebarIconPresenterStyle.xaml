<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:ctrl="clr-namespace:SidebarNav.Controls"
                    xmlns:vm="clr-namespace:SidebarNav.ViewModels"
                    xmlns:conv="clr-namespace:SidebarNav.Converters">

    <conv:IconTypeToVisibilityConverter x:Key="IconTypeToVisConverter"/>

    <Style TargetType="{x:Type ctrl:SidebarIconPresenter}">
        <Setter Property="Width" Value="{Binding IconSize, RelativeSource={RelativeSource Self}}"/>
        <Setter Property="Height" Value="{Binding IconSize, RelativeSource={RelativeSource Self}}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ctrl:SidebarIconPresenter}">
                    <Grid Width="{TemplateBinding Width}" Height="{TemplateBinding Height}">
                        <!-- Path Data 图标 -->
                        <Path x:Name="PathIcon"
                              Data="{TemplateBinding PathData}"
                              Fill="{TemplateBinding IconBrush}"
                              Stretch="Uniform"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                              Visibility="{Binding IconType, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource IconTypeToVisConverter}, ConverterParameter=PathData}"/>

                        <!-- Font Icon -->
                        <TextBlock x:Name="FontIcon"
                                   Text="{TemplateBinding FontIconGlyph}"
                                   FontFamily="{TemplateBinding FontIconFamily}"
                                   FontSize="{TemplateBinding IconSize}"
                                   Foreground="{TemplateBinding IconBrush}"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Visibility="{Binding IconType, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource IconTypeToVisConverter}, ConverterParameter=FontIcon}"/>

                        <!-- Image 图标 -->
                        <Image x:Name="ImageIcon"
                               Width="{TemplateBinding IconSize}"
                               Height="{TemplateBinding IconSize}"
                               Stretch="Uniform"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Visibility="{Binding IconType, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource IconTypeToVisConverter}, ConverterParameter=Image}">
                            <Image.Style>
                                <Style TargetType="Image">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ImageSource, RelativeSource={RelativeSource TemplatedParent}}" Value="{x:Null}">
                                            <Setter Property="Source" Value="{x:Null}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Image.Style>
                        </Image>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
